name: Test Coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install coverage
        if [ -f requirements/linux/requirements.txt ]; then pip install -r requirements/linux/requirements.txt; fi
    
    - name: Run tests with coverage
      run: |
        coverage run -m unittest discover -s tests
        coverage report
        coverage xml
    
    - name: Extract coverage percentage
      id: coverage
      run: |
        COVERAGE=$(coverage report | grep 'TOTAL' | awk '{print $4}' | sed 's/%//')
        echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
    
    - name: Update coverage badge in README
      uses: actions/github-script@v6
      with:
        script: |
          const fs = require('fs');
          const coverage = ${{ steps.coverage.outputs.coverage }};
          const badge = `![Coverage](https://img.shields.io/badge/coverage-${coverage}%25-brightgreen)`;
          const readmePath = 'README.md';
          let readme = fs.readFileSync(readmePath, 'utf8');
          readme = readme.replace(/<!-- coverage-badge -->/, badge);
          fs.writeFileSync(readmePath, readme);
    
    - name: Commit and push if changed
      run: |
        git config user.name github-actions
        git config user.email github-actions@github.com
        git add README.md
        git diff --staged --quiet || git commit -m "chore: update coverage badge"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
